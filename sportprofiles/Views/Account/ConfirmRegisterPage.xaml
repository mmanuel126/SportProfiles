<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="sportprofiles.Views.Account.ConfirmRegisterPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title=" ">
    <VerticalStackLayout>

        <StackLayout Padding="0,13,0,0">
            <Image
                x:Name="imgLogo"
                HeightRequest="40"
                HorizontalOptions="Center"
                Source="logo.jpg"
                VerticalOptions="Center"
                WidthRequest="160" />

            <Label
                x:Name="lblAppName"
                Padding="25,5,25,5"
                FontAttributes="Bold"
                FontSize="18"
                HorizontalTextAlignment="Center"
                Text="Registration Confirmation"
                TextColor="{StaticResource Gray900}" />

            <Label
                x:Name="lblInstruction"
                Padding="20,5,20,5"
                FontAttributes="None"
                FontSize="15"
                HorizontalTextAlignment="Center"
                Text="Thank you for signing up on Sport Profiles! A confirmation email was sent to you at the email you provided. Please check your email and click on the confirmation link in the email to complete your registration. Then return here and tap the 'Return to Login' link below to Log in using your new credentials."
                TextColor="{StaticResource Gray600}" />

            <Label
                x:Name="ReturnToLogin"
                Padding="30,10,30,10"
                FontAttributes="Bold"
                FontSize="17"
                HorizontalOptions="CenterAndExpand"
                Text="Return to Login"
                TextColor="Blue">
                <Label.GestureRecognizers>
                    <TapGestureRecognizer Buttons="Primary" Tapped="Register_confirm_tap_Tapped" />
                </Label.GestureRecognizers>
            </Label>

            <Label Text="" />
        </StackLayout>

    </VerticalStackLayout>
</ContentPage>
